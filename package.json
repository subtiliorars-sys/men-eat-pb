{
  "name": "men-eat-pb",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:admin": "vite --open /admin.html",
    "start": "vite --open",
    "build": "vite build",
    "build:admin": "vite build --config vite.admin.config.ts",
    "preview": "vite preview",
    "test": "vitest run",
    "verify": "tsc --noEmit && vitest run && vite build && vite build --config vite.admin.config.ts",
    "package:itch": "powershell -ExecutionPolicy Bypass -File ./scripts/package-itchio.ps1"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.4.19",
    "vitest": "^2.1.9"
  }
}
